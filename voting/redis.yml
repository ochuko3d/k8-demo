apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    name: redis-pod
    app: demo-app
spec:
  containers:
    - name: redis-pod
      image: redis
      ports:
       - containerPort: 6379